//! Config data model, schema, validation, merge, migrations, state store, config service. Plan Phase 2.
//!
//! Skeleton crate — contents arrive with its migration phase; see MIGRATION_PLAN.md.

pub mod merge;
pub mod schema;
pub mod types;
pub mod validate;

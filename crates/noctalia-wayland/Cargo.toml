[package]
name = "noctalia-wayland"
description = "Wayland core: connection (client_system backend for EGL), outputs, seat, layer/popup surfaces, clipboard, foreign toplevels, text input."
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

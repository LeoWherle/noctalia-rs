# PROGRESS.log — append-only session journal for the Rust migration.
# One dated entry per session, newest at the BOTTOM. Never edit old entries.
# Template:
#   ## YYYY-MM-DD (session N)
#   Attempted: ...
#   Done: ... (task IDs from MIGRATION_PLAN.md)
#   Half-finished: ... (exact state, what compiles, what's stubbed)
#   Next step: ... (concrete enough to start cold)

## 2026-07-28 (session 1)
Attempted: Phase 0 — migration infrastructure, no logic ported.
Done: task 0.1 — MIGRATION_PLAN.md (17 phases, crate decisions, sandbox red flags,
  architecture decisions: calloop main loop + tokio sidecar, wayland client_system
  backend for EGL, pangocairo FFI for text). CLAUDE.md (quality bar + standing rules).
  This file. flake.nix gained rust-overlay input + devShells.rust
  (nix/rust-devshell.nix: pinned toolchain, bindgenHook, all C libs for FFI). .envrc
  (`use flake .#rust`). justfile gained `check` (fmt + clippy -D warnings + test).
  Cargo workspace skeleton: 13 empty crates under crates/ matching the phase layers
  (core, config, theme, ipc, system, dbus, net, calendar, audio, compositors, wayland,
  render, ui) + noctalia-shell bin + noctalia-qalc-sys placeholder. Workspace lints
  deny unwrap_used/expect_used.
Half-finished: nothing.
Next step: WAIT for user go-ahead on MIGRATION_PLAN.md (explicitly requested — do not
  start Phase 1 without it). After go-ahead: task 1.1 (port src/core/log.{cpp,h} to
  noctalia-core::log on tracing).

## 2026-07-28 (session 2)
Attempted: audit-and-replan pass on dependency strategy (user directive). No porting.
Done: replaced the fixed "C lib -> crate" decision table in MIGRATION_PLAN.md with a
  two-phase strategy: Phase A = FFI to the same C libs Noctalia uses (parity
  baseline; C++-only deps with no C ABI -> toml/serde_json/zbus; vendored
  third_party C compiled via cc), Phase B = measured pure-Rust swaps, tagged
  [future-candidate], gated on full-shell cutover + profiling (new Phase 17, tasks
  B.1-B.14, incl. allocator and GLES-binding decisions — both provisional until
  then). Retagged affected tasks (3.4, 5.5, 7.1-7.6, 8.3, 11.2, 12.4, 14.1).
  CLAUDE.md: standing rule 6 (FFI-first, never swap deps "while you're in there")
  + quality-bar dep bullet rewritten. nix/rust-devshell.nix: added curl + md4c
  (now Phase A FFI targets), comments updated; shell re-evaluates OK.
Half-finished: nothing.
Next step: WAIT for user confirmation of the revised MIGRATION_PLAN.md/CLAUDE.md
  diff. After go-ahead: task 1.1 (src/core/log.{cpp,h} -> noctalia-core::log).

## 2026-07-28 (session 3)
Attempted: task 1.1 — logging.
Done: 1.1. `noctalia-core::log` (crates/noctalia-core/src/log.rs): console sink
  (ANSI, time-only prefix, filtered by a global level) + file sink (plain, full-date
  prefix, always written, 1 MiB rotation to `.log.1`, 8 KiB per-line cap with
  UTF-8-safe truncation + short-suffix fallback, buffered flush: immediate on
  warn/error, else every 64 lines or 500ms) — a 1:1 port of src/core/log.{cpp,h},
  same constants, same format strings. `Logger` (per-section) + free
  log_debug!/log_info!/log_warn!/log_error! macros bypass tracing's dispatch and
  call the shared `log_message` sink directly: tracing's `target:` argument is
  baked into a `static` calls-site (verified in tracing-0.1.44/src/macros.rs), so it
  must be const-evaluable and can't carry a runtime `&'static str` section. A
  separate `install_tracing_bridge()` (tracing_subscriber::Layer) forwards ordinary
  `tracing::{debug,info,warn,error}!` calls from anywhere else in the process into
  the same sink (target = section), which is what "on tracing" buys future
  modules. `libc::localtime_r`/`libc::atexit` used for exact parity with the C++'s
  localtime_r/atexit flush registration (both behind SAFETY comments). One bug
  caught by the mandatory pre-done review (rule 3): the console write wasn't
  covered by the same lock as the file write (C++'s single `scoped_lock` spans
  both); fixed by widening the `FILE_STATE` mutex's scope over the whole
  `log_message` body. Tests: unit tests in log.rs for format-string parity
  (console/file prefix, ANSI codes, capping, UTF-8 boundary safety) +
  crates/noctalia-core/tests/log_test.rs, a direct port of tests/log_test.cpp
  (level parsing, env var filtering incl. case-sensitivity/invalid-value handling,
  capped-line file test, rotation test) as one sequential #[test] (matches the
  C++ test's single-threaded main() — this and the format tests are the only
  things touching the shared globals/env vars, each in its own test binary
  process, so no races), plus one added test proving the tracing bridge path.
  Added deps: tracing, tracing-subscriber, libc (checked: cargo tree -i
  openssl-sys -> nothing depends on it).
Half-finished: nothing.
Next step: task 1.2 (src/core/files/* -> core::files).

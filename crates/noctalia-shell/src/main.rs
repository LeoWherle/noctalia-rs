//! The noctalia binary. Skeleton only — the CLI arrives in Plan Phase 4, the
//! real entry point in task 16.5. See MIGRATION_PLAN.md.

fn main() {
    eprintln!("noctalia-rs: migration skeleton -- nothing to run yet (see MIGRATION_PLAN.md)");
    std::process::exit(1);
}
